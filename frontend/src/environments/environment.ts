// Ambiente de DESENVOLVIMENTO (padrão usado por `ng serve` e build dev).
// É este arquivo que o Angular carrega quando NÃO há substituição configurada.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};