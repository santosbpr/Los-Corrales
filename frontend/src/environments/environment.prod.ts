// Ambiente de PRODUÇÃO. Substitui environment.ts automaticamente em `ng build --configuration production`
// graças ao fileReplacements no angular.json.
export const environment = {
  production: true,
  apiUrl: 'https://los-corrales-api.onrender.com/api',
};